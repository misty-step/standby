# Premise: Standby Real Meeting Path

Created: 2026-06-17

Sanitized user request:

- Make Standby actually work for live meetings rather than the current demo.
- Target Microsoft Teams first.
- Preserve the long-term goal that Standby works with any call the user takes
  in any app on the computer.
- Approved proposal cards must dispatch to real local worker agents; the
  approval path must not be stubbed or backed by mock completions.
- Use research and shaping before implementation so the next build slice has a
  concrete proof loop and can be executed by a fresh delivery agent.

Privacy note: this premise is a short written summary of the request. It does
not include raw meeting audio, private transcript excerpts, credentials,
calendar data, or meeting URLs.

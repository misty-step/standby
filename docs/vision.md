# Standby Vision

The canonical project vision now lives at the repository root:
[`VISION.md`](../VISION.md).

This file is retained only as a pointer so older references resolve. Do not edit
it — update `VISION.md` instead (via `/vision`).
